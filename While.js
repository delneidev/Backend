const nomesUsuarios = ['Joana', 'Miguel', 'Luis', 'Pedro'];
let indice= 0;
const indiceAtual = nomesUsuarios.length // Inicializa o contador
console.log ({indiceAtual})

while (indice <nomesUsuarios.length ) {
    console.log(nomesUsuarios[indice]); // Exibe o nome no índice atual
    indice++; // Incrementa o contador
}

